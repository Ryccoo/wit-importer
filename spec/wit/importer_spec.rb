require "spec_helper"

RSpec.describe Wit::Importer do
  it "has a version number" do
    expect(Wit::Importer::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
